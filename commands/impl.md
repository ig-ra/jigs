---
description: Implement a plan via the igr:dev impl method (codex session, TDD, tunable gate/squash).
argument-hint: "<plan-path-or-worktree> [free-form execution tweaks]"
---
Run the **impl** method on: $ARGUMENTS

Invoke the `igr:dev` skill and follow `references/impl.md`. Extra text = free-form execution tweaks + plan context (e.g. "do not run the full gate after each task; squash then gate once").
