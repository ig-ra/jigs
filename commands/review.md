---
description: Review an implementation diff via the igr:dev review method (/simplify + /igr:code-review-skip-simplify, then apply fixes).
argument-hint: "[effort-level: default xhigh]"
---
Run the **review** method on: $ARGUMENTS

Invoke the `igr:dev` skill and follow `references/review.md`. Argument = the `/igr:code-review-skip-simplify` effort level (default `xhigh`).
