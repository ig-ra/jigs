---
description: Harden an idea (or existing spec) into a SPEC-SOUND spec via the igr:dev brainstorm method (census + per-angle codex loop + clean-rewrite).
argument-hint: "<idea-or-spec-path> [-- settled decisions to protect]"
---
Run the **brainstorm** method on: $ARGUMENTS

Invoke the `igr:dev` skill and follow `references/brainstorm.md` end to end. If the input is already a spec, skip production and go straight to hardening. Text after `--` = owner-settled decisions to protect from re-litigation. Never git-commit docs (the owner commits).
