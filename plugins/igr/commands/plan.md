---
description: Harden a spec into an implementation plan via the igr:dev plan method (code census -> mechanical diffs -> judgment angles-till-SOLID -> broad pass).
argument-hint: "<spec-path> [-- settled decisions to protect]"
---
Run the **plan** method on: $ARGUMENTS

Invoke the `igr:dev` skill and follow `references/plan.md` end to end:
P0 scope (inline) -> P1 code census (spawn the `igr:code-census` agent — LSP-native, read-only, self-audit built in; or a `general-purpose` subagent on model sonnet as fallback — for a schema + transitive-closure walk via LSP) -> P2 `superpowers:writing-plans` inline (write FROM the census; cite rows; defer the mechanical tail; whole-body for behavior-sensitive symbols) -> P3a mechanical pre-pass (inline deterministic diffs vs census/LSP) -> P3b `/igr:codex-adversarial-loop` per judgment angle (contracts -> behavior -> green-ordering), each looped till SOLID -> P3c broad full-plan pass till PLAN-SOUND.

Text after `--` = owner-settled decisions to protect from re-litigation. Never git-commit the plan or census (the owner commits docs).
