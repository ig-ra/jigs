---
description: Harden an idea (or existing spec) into a SPEC-SOUND spec via the igr:dev brainstorm method (census + per-angle codex loop + clean-rewrite).
argument-hint: "<idea-or-spec-path> [-- settled decisions to protect]"
---
Run the **brainstorm** method on: $ARGUMENTS

Invoke the `igr:dev` skill and follow `references/brainstorm.md` end to end. If the input is already a spec, skip production and go straight to hardening.

Before hardening, write the immutable `## Mental Model` contract (as-designed changeset inventory) into the spec and STOP for the owner's Drift-A review (intent vs model); freeze it only on sign-off. After the exit gate, re-derive the model and report Drift-B (hardened vs contract) in chat — never editing the contract. See `references/brainstorm.md` §"Mental Model contract" and §7.

Text after `--` = owner-settled decisions to protect from re-litigation. Never git-commit docs (the owner commits).
